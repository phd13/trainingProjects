# Test project "Users Modal Table"
## Install packages
```sh
npm i
```
If you want to see how table works with usersData from JSON, 
proceed to /nodeServer directory in this repository and execute 
the ```node server.js``` command.